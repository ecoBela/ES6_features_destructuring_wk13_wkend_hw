_8c9‍.x([["default",()=>_8c9‍.o]]);class Library {
    constructor (books = []) {
      this.books = books;
    };

    bookCount() {
      return this.books.length;
    };

    addBook(newBook) {
      this.books.push(newBook);
    };
    
    addBooks(newBooks) {
      newBooks.forEach(book => this.books.push(book));
    };
    
    printInventory() {
      this.books.forEach(({author, title}) => {
        console.log(`${title} by ${author}`);
      });
    };


};





_8c9‍.d(Library);
